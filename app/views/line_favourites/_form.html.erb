<%= form_for(@line_favourite) do |f| %>
  <% if @line_favourite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_favourite.errors.count, "error") %> prohibited this line_favourite from being saved:</h2>

      <ul>
      <% @line_favourite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.text_field :location_id %>
  </div>
  <div class="field">
    <%= f.label :favorite_id %><br>
    <%= f.text_field :favorite_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
